
Directories:
	./rom/		- Contains the contents of the eight LUTs used by DA.
	./reports/
		\--	output.rpt	- Values of ACC_OUT after sixteen iterations.
		\--	input.rpt	- Values of Addresses over sixteen iterations.
		\--	error.rpt	- (Match/Error) of python scripts expected value.
						versus the value seen in output.rpt.
	./sim/		- Contains "runsim.do", "wavesim.do", and "waveformat.do"

