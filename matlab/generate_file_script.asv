coefficients = fopen('../qsim_rtl/fir_filter/reports/coefficients.rpt','r');
inputs = fopen('../qsim_rtl/fir_filter/reports/inputs.rpt','r');

%random values between zero and 8
b_intermediate = fscanf(coefficients,'%d');
x_intermediate = fscanf(coefficients,'%d');

%convolved signal
y = filter_function(x,b);

fprintf(output, '%d ',y);

figure
hold on
plot(y)
plot(x)
plot(b)